---
phase: 01-需求征集模块
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
autonomous: true
requirements: ["DEM-01"]
must_haves:
  truths:
    - "切换到「信息办管理员」角色可进入 collection-create 视图看到征集配置表单"
    - "表单含征集批次标题、说明、开始/截止日期、接收单位多选、发送方式、流程开关（是否需要专家评审等 toggle）"
    - "点击「发起征集并发送通知」后页面跳转至 notification-create 视图且字段已预填征集批次信息"
    - "提交前前端校验截止日期晚于开始日期、至少勾选 1 个接收单位、标题非空"
    - "暂存草稿按钮调用 saveDraft 并 toast 成功"
  artifacts:
    - path: "my-project-wiki/raw/docs/demo-0408/shared/views/demand.js"
      provides: "改造后的 collection-create 视图，含 D-02/D-03 流程开关与跳转预填逻辑"
      contains: "registerView('collection-create'"
  key_links:
    - from: "collection-create 视图 (demand.js line ~465)"
      to: "notification-create 视图"
      via: "navigate('notification-create', {prefill:{...}})"
      pattern: "navigate\\('notification-create'"
    - from: "collection-create 提交按钮"
      to: "DATA.collectionPlans"
      via: "push/unshift 新征集方案记录"
      pattern: "DATA\\.collectionPlans"
---

<objective>
改造 collection-create 视图（发起征集配置页），实现 DEM-01 闭环：配置时间/通知范围/流程开关 → 提交 → 跳转 notification-create 预填。

Purpose: Phase 1 入口节点。信息办管理员发起征集后，整个流程才能向下游推进。D-03 要求流程节点在征集表单内嵌开关，不跳转配置模块。
Output: demand.js 中 collection-create 视图改造为符合 UI-SPEC 视图 A 的字段清单 + D-02/D-03 交互。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-需求征集模块/01-CONTEXT.md
@.planning/phases/01-需求征集模块/01-RESEARCH.md
@.planning/phases/01-需求征集模块/01-UI-SPEC.md
@.planning/phases/01-需求征集模块/01-01-SUMMARY.md
@my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
@my-project-wiki/raw/docs/demo-0408/shared/views/notification.js
@my-project-code/CLAUDE.md

<interfaces>
// 现有 collection-create 视图在 demand.js line 465 已注册，本 Plan 改造其字段清单和提交逻辑

registerView('collection-create', renderFn): 已存在
navigate('notification-create', {prefill: object}): notification.js line ~189 已实现 prefill 读取
saveDraft(formId: string, data: object): core.js 已提供，写 localStorage
loadDraft(formId: string): object | null
clearDraft(formId: string): void
toast(msg, type): core.js
VIEWS: 全局对象，所有 registerView 注册的视图索引
DATA.collectionPlans: Array，格式参考 data.js 现有结构
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 collection-create 视图字段清单 + 流程开关</name>
  <files>my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js (重点读 line 465-554 collection-create 现有实现；line 555-792 collection-detail 参考 Tab 结构)
    - my-project-wiki/raw/docs/demo-0408/shared/core.js (renderStepWizard、saveDraft、loadDraft、breadcrumb 签名)
    - .planning/phases/01-需求征集模块/01-UI-SPEC.md (视图 A 字段清单与底部操作区章节)
    - .planning/phases/01-需求征集模块/01-CONTEXT.md (D-02、D-03、D-04、D-08 决策)
  </read_first>
  <action>
定位 demand.js `registerView('collection-create', ...)` 块，完整替换其 render 函数体。新实现要点：

1. 页面结构：单页表单（非四步向导），顶部 `breadcrumb('需求征集','发起征集')` + `<h2 class="page-title">发起需求征集</h2>`

2. 必须包含的字段（使用精确 id，供后续 Task 2 提交函数读取）：
   - `<input id="cc-title" type="text" maxlength="60">` 征集批次标题（必填），placeholder='2026年信息化项目需求征集'
   - `<textarea id="cc-desc" rows="3">` 征集说明（选填）
   - `<input id="cc-start" type="date">` 开始日期（必填）
   - `<input id="cc-end" type="date">` 截止日期（必填）
   - `<input id="cc-file" type="file">` 附件上传（选填，仅演示不真实上传）
   - `<div id="cc-units">` 通知接收单位（必填，至少 1 项），checkbox group 预设 8 项：
     value 分别为：教务处 / 招生处 / 科研处 / 学工处 / 党政办 / 图书馆 / 信息化办 / 资产处
     每个 checkbox: `<input type="checkbox" value="教务处">` 包裹在 `<label>`
   - `<input id="cc-contact" type="text">` 通知联系人，默认值预填 `roleDisplayName(getCurrentRole())` 若为空则填 '林已杰 20054379'
   - `<div id="cc-send-mode">` 发送方式 radio group name="cc-send-mode" 三选项：
     value='system' 系统消息 / value='system+dd' 系统+钉钉 / value='system+dd+sms' 系统+钉钉+短信
   - 流程开关区块（D-03）—— 以 `<fieldset class="card">` 包裹：
     `<legend>流程配置</legend>`
     `<label><input type="checkbox" id="cc-flow-sort" checked> 单位排序环节</label>`
     `<label><input type="checkbox" id="cc-flow-approve" checked> 单位领导审批环节</label>`
     `<label><input type="checkbox" id="cc-flow-expert"> 专家评审环节（Phase 3 后启用）</label>`
     `<label><input type="checkbox" id="cc-flow-notify" checked> 遴选结果通知</label>`
   - 流程模板 select（只读）：默认「标准需求征集流程」(disabled)
   - 关键字段旁加 tooltip `<span class="tooltip-icon" data-tip="...">?</span>`（D-08），至少给下列 4 字段加：
     - 征集批次标题：data-tip='建议格式「年度+项目类型+需求征集」'
     - 截止日期：data-tip='Demo 不做时间锁定，任何时候均可操作'
     - 通知接收单位：data-tip='将自动向所选单位的系统管理员发送征集通知'
     - 专家评审环节：data-tip='Phase 3 实现，当前仅展示开关占位'

3. 底部操作区：
   - `<button class="btn" onclick="_ccSaveDraft()">暂存草稿</button>`
   - `<button class="btn btn-primary" onclick="_ccSubmitAndJump()">发起征集并发送通知</button>`

4. render 函数末尾（返回 HTML 字符串前）读取草稿并在返回后由 setTimeout 填回，或使用 onload 钩子：
   ```
   var draft = loadDraft('collection-create') || {};
   // 生成 HTML 时将 draft.title 等填入 value 属性
   ```

5. 移除旧 render 中的 renderStepWizard 调用（若存在），改为单页表单。

禁止：
- import / require / npm（CLAUDE.md 强制）
- 内联 style="" 属性（必要时注释说明原因）
- 新增 CDN
- 自定义 modal / toast
  </action>
  <verify>
    <automated>grep -c "cc-title\|cc-start\|cc-end\|cc-units\|cc-flow-expert\|cc-flow-sort\|cc-flow-approve\|cc-flow-notify" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 中 `registerView('collection-create'` 存在
    - demand.js 包含以下精确字符串：`cc-title`、`cc-desc`、`cc-start`、`cc-end`、`cc-units`、`cc-contact`、`cc-send-mode`、`cc-flow-sort`、`cc-flow-approve`、`cc-flow-expert`、`cc-flow-notify`
    - demand.js 包含 8 个单位字符串：教务处、招生处、科研处、学工处、党政办、图书馆、信息化办、资产处
    - demand.js 包含 `tooltip-icon` 或 `data-tip=` 字符串
    - demand.js 包含 `loadDraft('collection-create'`
    - demand.js 不包含 `import ` 或 `require(` 或 `<script src="http` 新引入
    - 浏览器打开 app.html 切换至信息办管理员 → 发起征集配置页，所有字段可输入，流程开关可勾选
  </acceptance_criteria>
  <done>collection-create 视图字段清单和流程开关符合 UI-SPEC 视图 A 和 D-03，草稿预加载可用。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 _ccSubmitAndJump 提交 + 跳转 notification-create 预填（DEM-01 / D-02）</name>
  <files>my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js (Task 1 修改后的版本，确认按钮 onclick 绑定名)
    - my-project-wiki/raw/docs/demo-0408/shared/views/notification.js (必须检索 `registerView('notification-create'` 并查看函数内如何读取 prefill：getViewParams('notification-create').prefill 或类似；提取 prefill 对象字段的精确名字如 title/type/recipients/contact；本 Task 的 prefill 字段名必须与该视图实际读取名一致)
    - .planning/phases/01-需求征集模块/01-RESEARCH.md (Code Examples「征集配置页提交后跳转通知页」章节)
  </read_first>
  <action>
在 demand.js 内（紧随 collection-create 的 registerView 之后，或文件顶部 IIFE 内）新增以下函数。若 notification-create 视图实际使用的 prefill 字段名与下面示例不同，以 notification.js 实际字段名为准。

```javascript
function _ccCollectFormData() {
  var unitBoxes = document.querySelectorAll('#cc-units input[type=checkbox]:checked');
  return {
    title: (document.getElementById('cc-title').value || '').trim(),
    desc: document.getElementById('cc-desc').value || '',
    startDate: document.getElementById('cc-start').value,
    endDate: document.getElementById('cc-end').value,
    units: Array.prototype.map.call(unitBoxes, function(el){return el.value;}),
    contact: document.getElementById('cc-contact').value,
    sendMode: (document.querySelector('input[name="cc-send-mode"]:checked') || {}).value,
    flow: {
      sort: document.getElementById('cc-flow-sort').checked,
      approve: document.getElementById('cc-flow-approve').checked,
      expert: document.getElementById('cc-flow-expert').checked,
      notify: document.getElementById('cc-flow-notify').checked
    }
  };
}

function _ccSaveDraft() {
  saveDraft('collection-create', _ccCollectFormData());
  toast('草稿已保存', 'success');
}

function _ccValidate(data) {
  if (!data.title) { toast('请填写「征集批次标题」', 'error'); return false; }
  if (!data.startDate || !data.endDate) { toast('请填写开始日期和截止日期', 'error'); return false; }
  if (new Date(data.endDate) <= new Date(data.startDate)) { toast('截止日期必须晚于开始日期', 'error'); return false; }
  if (!data.units || data.units.length === 0) { toast('请至少选择 1 个通知接收单位', 'error'); return false; }
  if (!data.sendMode) { toast('请选择发送方式', 'error'); return false; }
  return true;
}

function _ccSubmitAndJump() {
  var data = _ccCollectFormData();
  if (!_ccValidate(data)) return;

  var plan = {
    id: 'cp-' + Date.now(),
    year: (new Date(data.startDate)).getFullYear(),
    title: data.title,
    desc: data.desc,
    startDate: data.startDate,
    endDate: data.endDate,
    units: data.units,
    contact: data.contact,
    sendMode: data.sendMode,
    flow: data.flow,
    status: '征集中',
    createdBy: roleDisplayName(getCurrentRole()),
    createdAt: new Date().toISOString().slice(0, 10)
  };
  if (!Array.isArray(DATA.collectionPlans)) DATA.collectionPlans = [];
  DATA.collectionPlans.unshift(plan);
  logOperation('demand-collect', 'create', plan.title);
  clearDraft('collection-create');

  if (typeof VIEWS !== 'undefined' && VIEWS['notification-create']) {
    navigate('notification-create', {
      prefill: {
        title: plan.year + '年度信息化项目需求征集通知',
        type: 'collection-notice',
        recipients: plan.units,
        contact: plan.contact,
        sourcePlanId: plan.id
      }
    });
    toast('征集方案已发起，通知正在预填', 'success');
  } else {
    toast('Demo：通知功能待完善', 'info');
  }
}
```

若 notification.js 中 notification-create 视图读取的是 `params.prefill.recipientUnits`（而非 recipients），则相应改名；其他字段同理。保留 sourcePlanId 作为 Demo 可追溯字段。
  </action>
  <verify>
    <automated>grep -c "_ccSubmitAndJump\|_ccCollectFormData\|_ccValidate\|_ccSaveDraft" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 包含函数定义：`_ccCollectFormData`、`_ccSaveDraft`、`_ccValidate`、`_ccSubmitAndJump`（四个 grep 命中）
    - demand.js 包含 `navigate('notification-create'`
    - demand.js 包含 `DATA.collectionPlans.unshift` 或 `DATA.collectionPlans.push`
    - demand.js 包含 `clearDraft('collection-create'`
    - demand.js 包含 `logOperation('demand-collect'`
    - demand.js 包含对 data.endDate 与 data.startDate 的日期比较（grep `new Date(data.endDate)`）
    - 浏览器手动验证：切换角色「信息办管理员」→ 发起需求征集 → 填写所有必填字段 → 点击「发起征集并发送通知」 → 页面跳转至 notification-create 视图 → 通知标题、接收单位、联系人已预填
    - 缺字段提交时对应 toast 错误提示出现
  </acceptance_criteria>
  <done>提交逻辑校验通过后写入 DATA.collectionPlans 并跳转 notification-create 预填，满足 DEM-01 闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 角色切换 → 视图访问控制 | Demo 角色切换仅展示，不做强制访问控制（Out of Scope） |
| 表单输入 → DATA.collectionPlans 持久化 | 内存写入，刷新即丢失 |
| 跨视图 prefill 参数 → notification-create | 通过 navigate 传递对象引用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Input Validation | _ccValidate 未校验的字段（如 sendMode 值枚举） | mitigate | _ccValidate 检查标题/日期/单位/发送方式非空；日期比较用 Date 构造 |
| T-02-02 | XSS | plan.title 拼接进其他视图渲染 | mitigate | maxlength=60 限制；后续视图若用 innerHTML 渲染 title 应在 escape 后使用（归下游 Plan 执行者 read_first 时确认） |
| T-02-03 | Tampering | DATA.collectionPlans 全局可改 | accept | Demo 性质，Out of Scope |
| T-02-04 | Repudiation | 谁发起了征集 | mitigate | plan.createdBy 记录 + logOperation('demand-collect','create',title) |
| T-02-05 | IDOR | 角色未切换即触发 _ccSubmitAndJump | accept | 纯前端 Demo，菜单仅向 info-admin 暴露入口；非敏感操作 |
</threat_model>

<verification>
1. 浏览器打开 app.html，切换到「信息办管理员」
2. 进入「发起需求征集」，填写标题、开始/截止日期（截止晚于开始）、勾选 3 个单位、选择发送方式
3. 勾选「专家评审环节」toggle（D-03）
4. 点击「暂存草稿」→ toast「草稿已保存」，刷新页面后重新进入视图，字段恢复
5. 点击「发起征集并发送通知」→ 跳转 notification-create 视图，通知标题/接收单位/联系人已预填
6. 故意清空标题再提交 → toast「请填写「征集批次标题」」
7. 截止日期早于开始日期 → toast「截止日期必须晚于开始日期」
8. 不勾选任何单位 → toast「请至少选择 1 个通知接收单位」
</verification>

<success_criteria>
- DEM-01 完整可演示
- 符合 D-01/D-02/D-03/D-04/D-08
- 校验规则齐全
- DATA.collectionPlans 记录新增
</success_criteria>

<output>
创建 `.planning/phases/01-需求征集模块/01-02-SUMMARY.md` 记录：
- collection-create 视图改造的字段清单
- _cc* 函数清单和行号
- notification-create prefill 字段名（根据 read_first 实际确认的结果，供 Plan 06+ 参考）
- DATA.collectionPlans 新增记录的字段结构
</output>
