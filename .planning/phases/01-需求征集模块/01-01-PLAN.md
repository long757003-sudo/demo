---
phase: 01-需求征集模块
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-project-wiki/raw/docs/demo-0408/shared/data.js
  - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
  - my-project-wiki/raw/docs/demo-0408/app.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "DATA.tagLibrary 作为标签库唯一数据源存在，含四大类目预设标签"
    - "DATA.demandUsers 含三个演示账号（唐明 20053964、林已杰 20054379、王一凡 50240014）及其他 Mock 用户"
    - "roleDisplayName 映射返回真实演示姓名而非旧 Mock 名"
    - "切换角色为信息办管理员或单位系统管理员，可在菜单打开「标签库」视图并看到类目+标签列表"
    - "在标签库视图可新增一个标签，页面立即显示新标签（DATA.tagLibrary 已更新）"
    - "在标签库视图可删除一个非预设标签，toast 反馈成功"
  artifacts:
    - path: "my-project-wiki/raw/docs/demo-0408/shared/data.js"
      provides: "DATA.tagLibrary / DATA.demandUsers / 更新的 roleDisplayName 映射"
      contains: "DATA.tagLibrary"
    - path: "my-project-wiki/raw/docs/demo-0408/shared/views/demand.js"
      provides: "registerView('tag-library', ...) 完整实现"
      contains: "registerView('tag-library'"
    - path: "my-project-wiki/raw/docs/demo-0408/app.html"
      provides: "侧边栏/菜单加入标签库入口"
      contains: "tag-library"
  key_links:
    - from: "shared/views/demand.js (tag-library 视图)"
      to: "DATA.tagLibrary"
      via: "读取渲染 + 写入（新增/删除/编辑）"
      pattern: "DATA\\.tagLibrary"
    - from: "app.html 菜单"
      to: "tag-library 视图"
      via: "navigate('tag-library')"
      pattern: "navigate\\('tag-library'"
---

<objective>
建立 Phase 1 的数据基础层 + 标签库可交互视图（D-09）。

Purpose: 标签库是 Phase 1 新增模块，需求申请表的标签选择需要从 DATA.tagLibrary 动态取数，没有数据源就无法实现后续 DEM-03 的标签字段。同时补齐演示账号真实姓名/工号（D-10）的底层数据，避免所有后续视图各自硬编码。
Output: data.js 增加 tagLibrary/demandUsers/修正 roleDisplayName；demand.js 新增 tag-library 视图；app.html 菜单加入入口。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-需求征集模块/01-CONTEXT.md
@.planning/phases/01-需求征集模块/01-RESEARCH.md
@.planning/phases/01-需求征集模块/01-UI-SPEC.md
@my-project-wiki/raw/docs/demo-0408/shared/data.js
@my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
@my-project-wiki/raw/docs/demo-0408/app.html
@my-project-code/CLAUDE.md

<interfaces>
<!-- 核心函数签名（来自 demo-0408/shared/core.js），tag-library 视图必须复用 -->

registerView(id: string, renderFn: () => string): void
navigate(viewId: string, params?: object): void
getViewParams(viewId: string): object | null
toast(msg: string, type: 'success'|'error'|'warning'|'info', duration?: number): void
showModal(title: string, bodyHtml: string, buttons: string): void
closeModal(): void
showReturnDialog(title: string, onConfirm: (reason: string) => void): void
getCurrentRole(): string
logOperation(module: string, action: string, ...args: any[]): void

// 数据源（部分已存在）
DATA.demands: Array<{id, name, unitId, collectionId, sortOrder, status, ...}>
DATA.collectionPlans: Array<{id, year, status, ...}>
DATA.roles: Array<{id, name, displayName}>  // 已存在
DATA.operationLogs: Array<{module, action, operator, time, ...}>  // 由 logOperation 写入

// 本 Plan 需新增
DATA.tagLibrary: Array<{id: string, category: string, name: string, builtin: boolean, createdAt: string}>
DATA.demandUsers: Array<{id: string, name: string, empNo: string, unitId: string, roles: string[]}>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 data.js — 新增 tagLibrary、demandUsers，修正 roleDisplayName 映射</name>
  <files>my-project-wiki/raw/docs/demo-0408/shared/data.js</files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/data.js (完整读取，了解现有 DATA 对象结构和导出方式)
    - my-project-wiki/raw/docs/demo-0408/shared/core.js (检索 roleDisplayName 函数，确认是否在 core.js 而非 data.js)
    - .planning/phases/01-需求征集模块/01-RESEARCH.md (标签库预设类目建议章节、演示账号 Mock 数据规范章节)
  </read_first>
  <action>
在 data.js 末尾（或紧随 DATA 对象定义之后）新增以下三项数据：

1. DATA.tagLibrary = [
  // 业务领域 (8)
  {id:'tag-biz-01', category:'业务领域', name:'教学管理', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-02', category:'业务领域', name:'科研管理', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-03', category:'业务领域', name:'学生事务', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-04', category:'业务领域', name:'行政办公', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-05', category:'业务领域', name:'财务管理', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-06', category:'业务领域', name:'人事管理', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-07', category:'业务领域', name:'招生就业', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-biz-08', category:'业务领域', name:'图书档案', builtin:true, createdAt:'2026-01-01'},
  // 技术类型 (8)
  {id:'tag-tech-01', category:'技术类型', name:'数据分析', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-02', category:'技术类型', name:'移动端', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-03', category:'技术类型', name:'集成接口', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-04', category:'技术类型', name:'报表统计', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-05', category:'技术类型', name:'AI/智能化', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-06', category:'技术类型', name:'云部署', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-07', category:'技术类型', name:'安全合规', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-tech-08', category:'技术类型', name:'数据治理', builtin:true, createdAt:'2026-01-01'},
  // 建设类型 (5)
  {id:'tag-build-01', category:'建设类型', name:'全新建设', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-build-02', category:'建设类型', name:'功能扩展', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-build-03', category:'建设类型', name:'系统替换', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-build-04', category:'建设类型', name:'接口改造', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-build-05', category:'建设类型', name:'升级改造', builtin:true, createdAt:'2026-01-01'},
  // 项目规模 (4)
  {id:'tag-scale-01', category:'项目规模', name:'微型（<20万）', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-scale-02', category:'项目规模', name:'小型（20-100万）', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-scale-03', category:'项目规模', name:'中型（100-200万）', builtin:true, createdAt:'2026-01-01'},
  {id:'tag-scale-04', category:'项目规模', name:'重大（≥200万）', builtin:true, createdAt:'2026-01-01'}
];

2. DATA.demandUsers = [
  // 三个核心演示账号（D-10，260409 会议确认）
  {id:'u-tangming', name:'唐明', empNo:'20053964', unitId:'unit-info', roles:['info-leader','unit-leader']},
  {id:'u-linyijie', name:'林已杰', empNo:'20054379', unitId:'unit-info', roles:['info-admin','unit-sysadmin']},
  {id:'u-wangyifan', name:'王一凡', empNo:'50240014', unitId:'unit-edu', roles:['project-manager']},
  // 其他 Mock 用户供指派/选人列表筛选
  {id:'u-m01', name:'李小雨', empNo:'50240021', unitId:'unit-edu', roles:['project-manager']},
  {id:'u-m02', name:'陈建国', empNo:'50240035', unitId:'unit-sci', roles:['project-manager']},
  {id:'u-m03', name:'赵慧敏', empNo:'50240048', unitId:'unit-stu', roles:['project-manager']},
  {id:'u-m04', name:'周涛', empNo:'50240056', unitId:'unit-hr', roles:['project-manager']},
  {id:'u-m05', name:'孙丽', empNo:'50240063', unitId:'unit-lib', roles:['project-manager']},
  {id:'u-m06', name:'黄志强', empNo:'20054421', unitId:'unit-edu', roles:['unit-sysadmin']},
  {id:'u-m07', name:'吴芳', empNo:'20054438', unitId:'unit-sci', roles:['unit-sysadmin','unit-leader']}
];

3. 修正 roleDisplayName 映射：
   - 在 data.js 中定位 roleDisplayName（若在 core.js 则修正 core.js）
   - 将 info-leader 映射返回 '唐明'（而非 '王主任' 等旧 Mock 名）
   - 将 info-admin 映射返回 '林已杰'
   - 将 unit-leader 映射返回 '唐明'
   - 将 unit-sysadmin 映射返回 '林已杰'
   - 将 project-manager 映射返回 '王一凡'
   - 若原函数签名为 roleDisplayName(roleId): string，保持签名，仅修改返回值映射表

4. 若 DATA.demands 现有 mock 记录的 status 枚举不完整，补充覆盖八种状态至少各一条：
   draft / submitted / sorted / unit-approved / unit-rejected / in-selection / supported / not-supported
   （每种状态至少一条 mock 需求，用于后续视图筛选验证）

不使用 import/require/npm，维持纯 IIFE 或全局 DATA 赋值模式，与 data.js 现有风格一致。
  </action>
  <verify>
    <automated>grep -c "DATA.tagLibrary" my-project-wiki/raw/docs/demo-0408/shared/data.js &amp;&amp; grep -c "DATA.demandUsers" my-project-wiki/raw/docs/demo-0408/shared/data.js &amp;&amp; grep -c "20053964" my-project-wiki/raw/docs/demo-0408/shared/data.js &amp;&amp; grep -c "王一凡" my-project-wiki/raw/docs/demo-0408/shared/data.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DATA.tagLibrary" data.js` 返回 ≥ 1
    - `grep -c "DATA.demandUsers" data.js` 返回 ≥ 1
    - data.js contains the string `20053964` (唐明工号)
    - data.js contains the string `20054379` (林已杰工号)
    - data.js contains the string `50240014` (王一凡工号)
    - `grep -c "category:'业务领域'" data.js` 返回 ≥ 8
    - `grep -c "category:'技术类型'" data.js` 返回 ≥ 8
    - `grep -c "category:'建设类型'" data.js` 返回 ≥ 5
    - `grep -c "category:'项目规模'" data.js` 返回 ≥ 4
    - data.js 或 core.js 中 roleDisplayName 分支返回 '唐明'、'林已杰'、'王一凡'（grep 可找到这三个字符串）
    - data.js 中 DATA.demands 包含 status:'supported'、status:'not-supported'、status:'unit-approved'、status:'in-selection' 四种（至少）字符串
    - 浏览器控制台打开 app.html 后 `DATA.tagLibrary.length` ≥ 25，`DATA.demandUsers.length` ≥ 10
  </acceptance_criteria>
  <done>data.js 含 tagLibrary（≥25 条，4 类目）、demandUsers（≥10 条，含 3 个演示账号的真实姓名+工号）、roleDisplayName 返回演示姓名；demands mock 数据覆盖 8 种状态。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 tag-library 视图（D-09）+ 菜单入口</name>
  <files>
    my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
    my-project-wiki/raw/docs/demo-0408/app.html
  </files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js (读 lines 1-200 理解视图注册模式，读 lines 793-1054 的 demand-list 作为 T2 列表参考模板)
    - my-project-wiki/raw/docs/demo-0408/shared/core.js (检索 showModal、toast、closeModal、logOperation 签名)
    - my-project-wiki/raw/docs/demo-0408/app.html (理解菜单/侧边栏注入方式，查找 navigate('demand-list' 等现有菜单项作为参考位置)
    - my-project-wiki/raw/docs/demo-0408/shared/nav.js (如存在，理解菜单注册模式)
    - my-project-wiki/raw/docs/demo-0408/shared/style.css (查找 .data-table、.card、.btn、.tag 类定义，视图内使用)
    - .planning/phases/01-需求征集模块/01-UI-SPEC.md (Component Inventory、Color、Spacing、Typography 约束)
  </read_first>
  <action>
在 demand.js 现有所有 registerView 之后追加 `registerView('tag-library', function() { ... })`，实现一个 T2 列表风格的标签库管理视图（D-09）。

视图 HTML 结构（返回字符串）：
1. 顶部：面包屑 `breadcrumb('系统管理','标签库')` + 页标题 `<h2>标签库管理</h2>` + 说明文字「标签库是需求申请表标签字段的唯一数据源，新增/删除后立即在需求申请表生效」
2. 操作栏（card）：
   - 类目筛选 `<select id="tl-cat-filter">`：全部 / 业务领域 / 技术类型 / 建设类型 / 项目规模
   - 关键词搜索 `<input id="tl-kw">`
   - 「新增标签」按钮（class `btn btn-primary`）onclick 调用 `_tlOpenCreate()`
3. 主体：`<table class="data-table">` 列：序号 / 类目 / 标签名 / 类型（内置/自定义）/ 创建时间 / 操作（编辑 + 删除）
   - 数据源：`DATA.tagLibrary`，按 `document.getElementById('tl-cat-filter').value` 和 `tl-kw` 过滤
   - 内置标签删除按钮禁用（disabled + title='内置标签不可删除'）
   - 每行「编辑」按钮 onclick `_tlOpenEdit(id)`，「删除」onclick `_tlDelete(id)`
4. 空状态：当过滤结果为 0 时显示 `<div class="empty">暂无标签</div>`

配套 JS 函数（写在 demand.js 同一文件内，放在 registerView 外层 IIFE 或全局函数作用域，与文件现有函数共存）：

```javascript
function _tlRender() { renderView('tag-library'); }

function _tlOpenCreate() {
  var body = '<div style="padding:8px 0">' +
    '<div class="form-row"><label>类目 *</label>' +
      '<select id="tl-new-cat"><option>业务领域</option><option>技术类型</option><option>建设类型</option><option>项目规模</option></select></div>' +
    '<div class="form-row"><label>标签名 *</label><input id="tl-new-name" type="text" maxlength="20"/></div>' +
    '</div>';
  showModal('新增标签', body,
    '<button class="btn" onclick="closeModal()">取消</button>' +
    '<button class="btn btn-primary" onclick="_tlSubmitCreate()">保存</button>');
}

function _tlSubmitCreate() {
  var cat = document.getElementById('tl-new-cat').value;
  var name = (document.getElementById('tl-new-name').value || '').trim();
  if (!name) { toast('请填写标签名', 'error'); return; }
  if (DATA.tagLibrary.some(function(t){return t.category===cat && t.name===name;})) {
    toast('该类目下已存在同名标签', 'error'); return;
  }
  DATA.tagLibrary.push({
    id: 'tag-custom-' + Date.now(),
    category: cat, name: name, builtin: false,
    createdAt: new Date().toISOString().slice(0,10)
  });
  logOperation('tag-library', 'create', name);
  closeModal(); toast('标签已新增', 'success'); _tlRender();
}

function _tlOpenEdit(id) {
  var t = DATA.tagLibrary.find(function(x){return x.id===id;});
  if (!t) return;
  var body = '<div class="form-row"><label>标签名 *</label><input id="tl-edit-name" type="text" value="'+t.name+'" maxlength="20"/></div>';
  showModal('编辑标签', body,
    '<button class="btn" onclick="closeModal()">取消</button>' +
    '<button class="btn btn-primary" onclick="_tlSubmitEdit(\''+id+'\')">保存</button>');
}

function _tlSubmitEdit(id) {
  var t = DATA.tagLibrary.find(function(x){return x.id===id;});
  var newName = (document.getElementById('tl-edit-name').value || '').trim();
  if (!newName) { toast('请填写标签名', 'error'); return; }
  t.name = newName;
  logOperation('tag-library', 'edit', id, newName);
  closeModal(); toast('标签已更新', 'success'); _tlRender();
}

function _tlDelete(id) {
  var t = DATA.tagLibrary.find(function(x){return x.id===id;});
  if (!t || t.builtin) { toast('内置标签不可删除', 'warning'); return; }
  showModal('删除标签', '<p>确认删除标签「'+t.name+'」？已引用该标签的历史需求不受影响。</p>',
    '<button class="btn" onclick="closeModal()">取消</button>' +
    '<button class="btn btn-danger" onclick="_tlConfirmDelete(\''+id+'\')">确认删除</button>');
}

function _tlConfirmDelete(id) {
  var idx = DATA.tagLibrary.findIndex(function(x){return x.id===id;});
  if (idx >= 0) DATA.tagLibrary.splice(idx, 1);
  logOperation('tag-library', 'delete', id);
  closeModal(); toast('标签已删除', 'success'); _tlRender();
}
```

5. app.html：在现有菜单（参考 `navigate('demand-list')` 附近）加入「标签库」入口，仅对 info-admin、unit-sysadmin 两个角色显示。使用与既有菜单项相同的 `<a>` 或 `<li>` 结构 + `onclick="navigate('tag-library')"`。若菜单是通过 nav.js 构建，则在 nav.js 对应角色菜单数组加入 `{id:'tag-library', label:'标签库', view:'tag-library'}`。

严禁事项：
- 禁止 import/require/npm（CLAUDE.md 强制）
- 禁止内联 style="" 属性（除必要情况，需注释说明）
- 禁止引入新 CDN 库
- 禁止自定义 modal/drawer/toast（必须用 core.js 已有函数）
  </action>
  <verify>
    <automated>grep -c "registerView('tag-library'" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js &amp;&amp; grep -c "_tlSubmitCreate\|_tlConfirmDelete" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js &amp;&amp; grep -rn "navigate('tag-library'\|view:'tag-library'" my-project-wiki/raw/docs/demo-0408/app.html my-project-wiki/raw/docs/demo-0408/shared/nav.js 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - demand.js contains `registerView('tag-library'`
    - demand.js contains `_tlSubmitCreate`, `_tlOpenCreate`, `_tlDelete`, `_tlConfirmDelete`, `_tlOpenEdit`, `_tlSubmitEdit` (六个函数全部存在)
    - demand.js contains `DATA.tagLibrary.push` (新增逻辑)
    - demand.js contains `DATA.tagLibrary.splice` (删除逻辑)
    - demand.js contains `showModal('新增标签'` 和 `showModal('编辑标签'`
    - demand.js 中 tag-library 视图字符串包含 `data-table`（表格类）、`btn btn-primary`（按钮类）、`logOperation('tag-library'`
    - app.html 或 nav.js 中包含 `navigate('tag-library'` 或 `view:'tag-library'`
    - demand.js 中 tag-library 视图 HTML 包含筛选控件 id `tl-cat-filter` 和搜索 id `tl-kw`
    - 手动：在浏览器打开 app.html，切换到「信息办管理员」，菜单中可见「标签库」，点击进入可见表格 ≥25 行；点击「新增标签」弹 modal，填入并保存后列表多一行并 toast 成功
    - grep 检索 demand.js：不存在 `import ` / `require(` / `<style ` 新增
  </acceptance_criteria>
  <done>tag-library 视图注册完成，CRUD 全部可用，菜单入口对 info-admin/unit-sysadmin 显示，所有操作有 toast 反馈且 DATA.tagLibrary 同步更新。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器内存 → DATA 全局对象 | 纯前端 Demo，所有数据在内存；刷新即丢失（演示预期行为） |
| 用户输入 → innerHTML 渲染 | 标签名拼接进 modal/toast/视图字符串，存在 XSS 面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | DATA.tagLibrary 全局对象 | accept | Demo 演示用，角色切换仅演示，不提供生产级访问控制；roadmap Out of Scope 第 4 条已声明 |
| T-01-02 | Information Disclosure | 演示账号工号（50240014 等） | accept | 260409 会议确认的公开演示账号，非真实敏感信息 |
| T-01-03 | Input Injection (XSS) | _tlOpenEdit 将 t.name 拼接进 showModal 的 value 属性、_tlDelete 将 t.name 拼接进 modal body | mitigate | 标签名在新增/编辑时限制 maxlength=20 且 trim；拼接前可用 `.replace(/[<>"]/g,'')` 转义特殊字符；Demo 演示无对抗性输入风险但保留基础过滤 |
| T-01-04 | Repudiation | 新增/编辑/删除标签 | mitigate | 每个写操作调用 `logOperation('tag-library', action, ...)` 写入 DATA.operationLogs |
| T-01-05 | Denial of Service | 批量新增超大标签 | accept | 纯前端 Demo，用户自伤不纳入威胁模型 |
</threat_model>

<verification>
1. 浏览器打开 `my-project-wiki/raw/docs/demo-0408/app.html`
2. 控制台执行 `DATA.tagLibrary.length` ≥ 25，`DATA.demandUsers.find(u=>u.empNo==='50240014').name === '王一凡'`
3. 切换角色「信息办管理员」，侧边栏可见「标签库」菜单
4. 点击进入，看到按类目分组的表格，≥25 行
5. 点击「新增标签」，填入「测试-业务领域-新标签」，保存；列表多出一行，toast 成功
6. 点击该行「删除」，确认删除，行消失，toast 成功
7. 点击任一内置标签的「删除」按钮应被禁用或 toast「内置标签不可删除」
</verification>

<success_criteria>
- DATA.tagLibrary、DATA.demandUsers 数据完整
- roleDisplayName 返回真实演示姓名
- tag-library 视图 CRUD 可用，菜单入口存在
- 所有变更符合 D-01（内嵌到 app.html 体系）、D-09（标签库完整实现）、D-10（演示账号）
- 无 import/require/npm/新 CDN
</success_criteria>

<output>
创建 `.planning/phases/01-需求征集模块/01-01-SUMMARY.md` 记录：
- DATA.tagLibrary 条目总数和各类目数
- DATA.demandUsers 条目总数和演示账号验证
- tag-library 视图实现要点和函数清单
- 菜单注入位置（app.html vs nav.js）
- 后续 Plan 02-05 依赖此 Plan 提供的 DATA.tagLibrary 和 DATA.demandUsers
</output>
