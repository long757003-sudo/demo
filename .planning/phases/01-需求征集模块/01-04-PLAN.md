---
phase: 01-需求征集模块
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-03"]
files_modified:
  - my-project-code/demo/shared/views/demand.js
autonomous: true
requirements: ["DEM-04", "DEM-05"]
must_haves:
  truths:
    - "切换到「单位系统管理员」角色进入 demand-sort，看到本单位 submitted 状态的需求列表，每行有上移/下移按钮"
    - "点击上移/下移后 DATA.demands 对应条目的 sortOrder 字段更新，切走再切回排序保持"
    - "点击「提交单位领导审批」出现 confirm modal，确认后需求状态变为 unit-pending，toast 成功"
    - "切换到「单位领导」角色进入 demand-approve（T4），可见生命周期进度条、需求排序内嵌区块（含上移/下移）、审批意见面板、操作记录 Tab"
    - "审批意见必填，点「审批通过」状态置为 unit-approved；点「退回修改」弹 showReturnDialog 退回为 unit-rejected"
  artifacts:
    - path: "my-project-code/demo/shared/views/demand.js"
      provides: "demand-sort（D-13 上移下移）+ 新建 demand-approve（T4 详情 D-14）"
      contains: "registerView('demand-approve'"
  key_links:
    - from: "demand-sort / demand-approve 上下移按钮"
      to: "DATA.demands[x].sortOrder"
      via: "交换位置后重编号"
      pattern: "sortOrder"
    - from: "demand-approve 审批按钮"
      to: "DATA.demands[x].status"
      via: "置为 unit-approved / unit-rejected"
      pattern: "unit-approved|unit-rejected"
---

<objective>
改造 demand-sort（DEM-04，D-13 上移下移）并新建 demand-approve（DEM-05，D-14 单位领导 T4 审批详情页）。

Purpose: 单位内部排序 → 单位领导审批子流程。D-13 强制上下移按钮不引入拖拽库；D-14 要求领导审批页是独立 T4 结构。RESEARCH Pitfall 1 警告：禁止在 demand-sort 里用 isLeader 分支混合两种职责。
Output: demand-sort 变为纯排序视图；新增 demand-approve 视图（T4 模板）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-需求征集模块/01-CONTEXT.md
@.planning/phases/01-需求征集模块/01-RESEARCH.md
@.planning/phases/01-需求征集模块/01-UI-SPEC.md
@.planning/phases/01-需求征集模块/01-03-SUMMARY.md
@my-project-code/demo/shared/views/demand.js
@my-project-code/CLAUDE.md

<interfaces>
registerView('demand-sort', ...)  // line ~1242, 含 isLeader 分支（须拆除）
// 新增 registerView('demand-approve', ...)

showReturnDialog(title, onConfirm): core.js 退回对话框（含原因分类 select + textarea）
projectTypeTag(type) / budgetToType(budget) / deadlineClass(dateStr)
logOperation(module, action, ...)
breadcrumb(...items): string

DATA.demands 状态流转：
submitted → unit-pending (sysadmin 提交审批) → unit-approved | unit-rejected (leader) → in-selection → supported | not-supported
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 demand-sort 为纯排序视图（DEM-04 / D-13）</name>
  <files>my-project-code/demo/shared/views/demand.js</files>
  <read_first>
    - my-project-code/demo/shared/views/demand.js (重点 line 1242-1332 demand-sort 现有实现，识别并移除 isLeader 分支)
    - .planning/phases/01-需求征集模块/01-CONTEXT.md (D-13)
    - .planning/phases/01-需求征集模块/01-RESEARCH.md (Pattern 6 上下移排序、Pitfall 1 职责混淆、Pitfall 2 DATA 同步)
  </read_first>
  <action>
完整替换 `registerView('demand-sort', function() { ... })` 函数体。要求：

(A) render 函数入口：若 `getCurrentRole() !== 'unit-sysadmin'`，返回 `<div class="empty">请切换到单位系统管理员角色</div>`。禁止保留 isLeader 分支。

(B) 视图 HTML 字符串：
- `breadcrumb('需求征集','需求排序')`
- `<h2>单位需求排序</h2>`
- 摘要卡片 class `card`：本单位 submitted 状态需求数、总预算（各 d.budget 累加）、当前征集批次截止日期
- 排序表格 class `data-table`，列：序号 / 项目名称 / 负责人 / 预算+类型Badge / 摘要(截断 40字) / 操作
- 操作列：每行两个按钮 `<button class="btn btn-sm" onclick="_dsMove('<id>','up')">↑</button>` 和 `↓`；首行↑ disabled，末行↓ disabled
- 底部：`<button class="btn" onclick="_dsSave()">保存排序</button>` + `<button class="btn btn-primary" onclick="_dsSubmitForApproval()">提交单位领导审批</button>`
- 空状态：`<div class="empty">本单位暂无需求，无法排序。请先确认项目负责人已提交需求申请表。</div>`

(C) 新增函数（放在文件内合适位置）：

```javascript
function _dsGetList() {
  var user = DATA.demandUsers.find(function(u){ return u.roles && u.roles.indexOf('unit-sysadmin')>=0; });
  var unitId = user ? user.unitId : 'unit-edu';
  return DATA.demands
    .filter(function(d){ return d.unitId === unitId && d.status === 'submitted'; })
    .sort(function(a,b){ return (a.sortOrder||999) - (b.sortOrder||999); });
}

function _dsMove(demandId, dir) {
  var list = _dsGetList();
  var idx = list.findIndex(function(d){ return d.id === demandId; });
  if (idx < 0) return;
  var swap = dir === 'up' ? idx - 1 : idx + 1;
  if (swap < 0 || swap >= list.length) return;
  var tmp = list[idx]; list[idx] = list[swap]; list[swap] = tmp;
  list.forEach(function(d, i){ d.sortOrder = i + 1; });
  renderView('demand-sort');
}

function _dsSave() {
  logOperation('demand-sort','save', _dsGetList().length + '条');
  toast('排序已保存','success');
}

function _dsSubmitForApproval() {
  var list = _dsGetList();
  if (list.length === 0) { toast('本单位暂无可提交的需求','error'); return; }
  showModal('确认提交排序',
    '<p>确认提交？提交后需等待单位领导审批，如需修改请联系领导退回。</p>',
    '<button class="btn" onclick="closeModal()">取消</button>' +
    '<button class="btn btn-primary" onclick="_dsConfirmSubmit()">确认提交</button>');
}

function _dsConfirmSubmit() {
  var list = _dsGetList();
  list.forEach(function(d){ d.status = 'unit-pending'; });
  logOperation('demand-sort','submit-for-approval', list.length + '条');
  closeModal();
  toast('已提交单位领导审批','success');
  renderView('demand-sort');
}
```

严禁：
- 引入 Sortable.js 或其他拖拽库
- 使用 HTML5 `draggable=` 属性实现拖拽
- 在此视图内放审批意见 textarea 或 leader 逻辑（Pitfall 1）
  </action>
  <verify>
    <automated>grep -c "_dsMove\|_dsGetList\|_dsSave\|_dsSubmitForApproval\|_dsConfirmSubmit" my-project-code/demo/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 包含五个函数：`_dsGetList`、`_dsMove`、`_dsSave`、`_dsSubmitForApproval`、`_dsConfirmSubmit`
    - demand.js 中 demand-sort 视图字符串包含 `onclick="_dsMove(` 出现至少两次（上移与下移）
    - demand.js 中 demand-sort 视图不再包含字符串 `isLeader`
    - demand.js 中 `registerView('demand-sort'` 块内不包含字符串 `draggable="true"` 或 `draggable=true`
    - demand.js 不包含 `Sortable` 字符串
    - 手动：切换「单位系统管理员」→ 进入需求排序 → 看到本单位 submitted 需求 → 点↓移动第一条 → 再切走再切回，顺序保持 → 点击「提交单位领导审批」→ confirm modal → 确认 → toast → 列表此时应为空（状态已变 unit-pending）
  </acceptance_criteria>
  <done>demand-sort 改造为纯排序视图，D-13 上下移实现，DATA.demands.sortOrder 同步。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 demand-approve 视图 — T4 单位领导审批详情页（DEM-05 / D-14）</name>
  <files>my-project-code/demo/shared/views/demand.js</files>
  <read_first>
    - my-project-code/demo/shared/views/demand.js (Task 1 修改后版本；还要看 line 555-792 collection-detail 作为 T4 Tab 结构参考)
    - my-project-code/demo/shared/core.js (showReturnDialog 签名、breadcrumb、logOperation)
    - .planning/phases/01-需求征集模块/01-UI-SPEC.md (视图 F「单位领导审批详情页」章节，生命周期进度条规范、BR-04 不涉及本视图)
    - .planning/phases/01-需求征集模块/01-CONTEXT.md (D-12 审批开关默认开启、D-14 内嵌排序区块)
    - .planning/phases/01-需求征集模块/01-RESEARCH.md (Code Examples 「T4 生命周期进度条」章节、Pitfall 6 必须含操作记录 Tab)
  </read_first>
  <action>
在 demand.js 中新增 `registerView('demand-approve', function() { ... })`（放在现有所有 registerView 之后）。该视图是 T4 详情模板。

render 函数结构（返回 HTML 字符串）：

```javascript
registerView('demand-approve', function() {
  if (getCurrentRole() !== 'unit-leader') {
    return '<div class="empty">请切换到单位分管领导角色</div>';
  }
  var batch = DATA.collectionPlans[0] || { title: '当前征集批次', endDate: '' };
  var unitId = (DATA.demandUsers.find(function(u){return u.roles && u.roles.indexOf('unit-leader')>=0;})||{}).unitId || 'unit-edu';
  var list = DATA.demands
    .filter(function(d){ return d.unitId === unitId && (d.status === 'unit-pending' || d.status === 'unit-approved' || d.status === 'unit-rejected'); })
    .sort(function(a,b){ return (a.sortOrder||999) - (b.sortOrder||999); });
  var activeTab = window._daTab || 'content';
  return (
    breadcrumb('需求征集','单位领导审批') +
    '<h2>' + batch.title + ' — 单位领导审批</h2>' +
    '<div class="page-sub"><span class="tag tag-orange">待单位领导审批</span></div>' +
    _renderLifecycleBar('需求征集') +
    '<div class="tabs">' +
      '<button class="tab' + (activeTab==='content'?' active':'') + '" onclick="_daSwitchTab(\'content\')">审批内容</button>' +
      '<button class="tab' + (activeTab==='logs'?' active':'') + '" onclick="_daSwitchTab(\'logs\')">操作记录</button>' +
    '</div>' +
    (activeTab === 'content'
      ? _daRenderContent(list)
      : _daRenderLogs())
  );
});
```

附函数（新增）：

```javascript
function _renderLifecycleBar(currentStage) {
  var stages = ['需求征集','立项论证','采购','实施','验收','运维'];
  var idx = stages.indexOf(currentStage);
  var html = '<div class="lifecycle-bar">';
  stages.forEach(function(s, i) {
    var cls = i < idx ? 'done' : (i === idx ? 'current' : 'pending');
    html += '<div class="lifecycle-stage ' + cls + '"><span class="dot"></span><span class="label">'+s+'</span></div>';
    if (i < stages.length - 1) {
      html += '<div class="lifecycle-line ' + (i < idx ? 'done' : 'pending') + '"></div>';
    }
  });
  html += '</div>';
  return html;
}

function _daSwitchTab(tab) {
  window._daTab = tab;
  renderView('demand-approve');
}

function _daRenderContent(list) {
  var needNext = (typeof window._daNeedNext === 'undefined') ? true : window._daNeedNext;  // D-12 默认开启
  var rows = list.map(function(d, i) {
    var first = i === 0, last = i === list.length - 1;
    return '<tr>' +
      '<td>' + (d.sortOrder || (i+1)) + '</td>' +
      '<td>' + d.name + '</td>' +
      '<td>' + (d.submitter || '') + '</td>' +
      '<td>' + (d.budget || 0) + ' 万元 ' + (typeof projectTypeTag==='function' && typeof budgetToType==='function' ? projectTypeTag(budgetToType(d.budget||0)) : '') + '</td>' +
      '<td>' + ((d.summary||'').slice(0,40)) + '</td>' +
      '<td>' +
        '<button class="btn btn-sm" ' + (first?'disabled':'') + ' onclick="_daMove(\''+d.id+'\',\'up\')">↑</button> ' +
        '<button class="btn btn-sm" ' + (last?'disabled':'') + ' onclick="_daMove(\''+d.id+'\',\'down\')">↓</button>' +
      '</td>' +
      '</tr>';
  }).join('');
  if (list.length === 0) rows = '<tr><td colspan="6" class="empty">本单位暂无待审批需求</td></tr>';

  return '<section class="card">' +
    '<h3>需求排序（可调整）</h3>' +
    '<table class="data-table"><thead><tr>' +
      '<th>序号</th><th>项目名称</th><th>负责人</th><th>预算</th><th>摘要</th><th>操作</th>' +
    '</tr></thead><tbody>' + rows + '</tbody></table>' +
    '</section>' +
    '<section class="card">' +
      '<h3>审批意见</h3>' +
      '<textarea id="da-comment" rows="4" placeholder="请填写审批意见"></textarea>' +
      '<div class="form-row">' +
        '<label><input type="checkbox" id="da-need-next" ' + (needNext?'checked':'') + ' onchange="_daToggleNeedNext()"> 是否需要下一级审批</label>' +
      '</div>' +
      (needNext ? '<div class="form-row"><label>下一级审批人</label><select id="da-next-approver"><option value="info-admin">林已杰 20054379（信息办管理员）</option></select></div>' : '') +
      '<div class="actions">' +
        '<button class="btn btn-danger" onclick="_daReject()">退回修改</button>' +
        '<button class="btn btn-primary" onclick="_daApprove()">审批通过</button>' +
      '</div>' +
    '</section>';
}

function _daRenderLogs() {
  var logs = (DATA.operationLogs || []).filter(function(l){
    return l.module === 'demand-sort' || l.module === 'demand-approve' || l.module === 'demand-fill';
  });
  if (logs.length === 0) return '<div class="card"><div class="empty">暂无操作记录</div></div>';
  return '<div class="card"><table class="data-table"><thead><tr><th>时间</th><th>模块</th><th>动作</th><th>操作人</th></tr></thead><tbody>' +
    logs.map(function(l){
      return '<tr><td>'+(l.time||'')+'</td><td>'+l.module+'</td><td>'+l.action+'</td><td>'+(l.operator||'')+'</td></tr>';
    }).join('') + '</tbody></table></div>';
}

function _daGetList() {
  var unitId = (DATA.demandUsers.find(function(u){return u.roles && u.roles.indexOf('unit-leader')>=0;})||{}).unitId || 'unit-edu';
  return DATA.demands
    .filter(function(d){ return d.unitId === unitId && d.status === 'unit-pending'; })
    .sort(function(a,b){ return (a.sortOrder||999) - (b.sortOrder||999); });
}

function _daMove(id, dir) {
  var list = _daGetList();
  var idx = list.findIndex(function(d){return d.id===id;});
  if (idx < 0) return;
  var swap = dir === 'up' ? idx - 1 : idx + 1;
  if (swap < 0 || swap >= list.length) return;
  var tmp = list[idx]; list[idx] = list[swap]; list[swap] = tmp;
  list.forEach(function(d,i){ d.sortOrder = i + 1; });
  renderView('demand-approve');
}

function _daToggleNeedNext() {
  window._daNeedNext = document.getElementById('da-need-next').checked;
  renderView('demand-approve');
}

function _daApprove() {
  var comment = (document.getElementById('da-comment').value || '').trim();
  if (!comment) { toast('请填写审批意见后再提交','error'); return; }
  var list = _daGetList();
  list.forEach(function(d){ d.status = 'unit-approved'; d.unitLeaderComment = comment; });
  logOperation('demand-approve','approve', list.length+'条：'+comment);
  toast('审批通过，已进入信息办遴选环节','success');
  renderView('demand-approve');
}

function _daReject() {
  showReturnDialog('退回需求排序', function(reason) {
    var list = _daGetList();
    list.forEach(function(d){ d.status = 'unit-rejected'; d.unitLeaderComment = reason; });
    logOperation('demand-approve','reject', list.length+'条：'+reason);
    toast('已退回，单位系统管理员可重新排序','success');
    renderView('demand-approve');
  });
}
```

D-12 关键：`window._daNeedNext` 默认 true（首次访问视图时 typeof undefined → needNext = true）。用户可手动取消勾选跳过下一级审批；勾选时展示下一级审批人 select（此处仅展示「林已杰 20054379」作为 Demo 选项）。

RESEARCH Pitfall 6：T4 必须含「操作记录 Tab」，已通过 activeTab === 'logs' 实现。

生命周期进度条样式（需要在 style.css 或 demand.js 视图内联 `<style>` 块）：若 demo-0408 style.css 不含 .lifecycle-bar / .lifecycle-stage.current 等类，本 Task 可在 demand-approve 视图字符串开头注入一个 `<style>` 块定义这几个类（简单 flex 横排 + 圆点 + 连接线）。注意：这是视图内嵌样式而非 HTML 内联 style="" 属性（CLAUDE.md 禁止的是内联 style 属性，不是 `<style>` 标签）。
  </action>
  <verify>
    <automated>grep -c "registerView('demand-approve'" my-project-code/demo/shared/views/demand.js &amp;&amp; grep -c "_daApprove\|_daReject\|_daMove\|_daSwitchTab\|_daToggleNeedNext\|_renderLifecycleBar" my-project-code/demo/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 包含 `registerView('demand-approve'`
    - demand.js 包含六个函数：`_daApprove`、`_daReject`、`_daMove`、`_daSwitchTab`、`_daToggleNeedNext`、`_renderLifecycleBar`
    - demand.js 中 demand-approve 视图字符串包含 `da-comment`（审批意见 textarea id）
    - demand.js 中 demand-approve 视图字符串包含 `da-need-next`（D-12 开关 id）
    - demand.js 中 _daApprove 函数包含对 `comment` 的非空校验并对应调用 `toast('请填写审批意见`
    - demand.js 中 _daApprove 将状态置为 `unit-approved`；_daReject 将状态置为 `unit-rejected`
    - demand.js 中 demand-approve 视图包含 `操作记录` 或 `_daRenderLogs` 引用（Pitfall 6）
    - demand.js 中 _renderLifecycleBar 渲染 6 个阶段字符串：需求征集、立项论证、采购、实施、验收、运维（grep 全部 6 个命中）
    - 手动：切换「单位领导」→ 需求审批 → 看到 T4 结构（进度条+排序表+审批面板+操作记录 Tab）→ 不填意见点审批通过 → toast 报错 → 填写意见后点审批通过 → toast 成功，列表清空 → 切 Tab 到「操作记录」可见刚才的 approve 记录
    - 手动：在审批面板先点上移调整顺序 → 再审批通过 → 顺序保留
  </acceptance_criteria>
  <done>demand-approve 视图 T4 结构完整，审批通过/退回可用，生命周期进度条和操作记录 Tab 存在。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 角色 → 视图权限 | render 入口判断 currentRole，避免错角色误入 |
| 审批操作 → DATA.demands 状态变更 | 批量改状态，无回滚机制（Demo 预期） |
| _daNeedNext 跨 render 持久化 | 通过 window 全局状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Authorization | demand-approve 可被任意角色切换后访问 | mitigate | render 函数入口检查 `getCurrentRole() !== 'unit-leader'` 返回空状态；demand-sort 同理 |
| T-04-02 | Input Validation | 审批意见为空时通过 | mitigate | _daApprove 校验 comment 非空，空时 toast error 中止 |
| T-04-03 | State Integrity | _dsMove / _daMove 越界访问数组 | mitigate | swap 下标前后范围检查（< 0 或 >= length 直接 return） |
| T-04-04 | Repudiation | 审批无历史可追 | mitigate | 每次 approve/reject 调用 logOperation，操作记录 Tab 可见 |
| T-04-05 | XSS | d.name / d.summary / comment 拼接 HTML | mitigate | 下游 Plan 05/06 对 summary 使用 escape；本 Plan 接受 demand.summary 来自 Plan 03 已 maxlength 限制 |
| T-04-06 | IDOR | unitId 硬编码 unit-edu 兜底 | accept | Demo 级，演示单位统一映射到教务处 |
</threat_model>

<verification>
1. 打开 app.html，切换「单位系统管理员」
2. 进入需求排序，看到 Plan 03 提交的需求；点↓移动顺序；点「提交单位领导审批」→ 确认
3. 切换「单位领导」→ 进入「需求审批」
4. 看到 T4 结构：进度条、需求排序区块、审批面板、操作记录 Tab
5. 在需求排序区块内点↑调整顺序
6. 不填意见点「审批通过」→ toast「请填写审批意见后再提交」
7. 填写审批意见「同意本单位排序结果」→ 点审批通过 → toast 成功
8. 切 Tab 到「操作记录」→ 看到刚才的 approve 记录
9. 另一次流程：再提交一批需求→切领导→点「退回修改」→ showReturnDialog → 填退回原因 → toast 成功，DATA.demands 对应条目状态变 unit-rejected
</verification>

<success_criteria>
- DEM-04 符合 D-13 上下移
- DEM-05 符合 D-14 独立 T4 详情页
- demand-approve 含进度条 + 排序区块 + 审批面板 + 操作记录 Tab
- D-12 下一级审批开关默认开启
- 无拖拽库 / 无新 CDN / 无 import / 无内联 style 属性（<style> 标签除外）
</success_criteria>

<output>
创建 `.planning/phases/01-需求征集模块/01-04-SUMMARY.md` 记录：
- demand-sort 改造后行数和核心函数
- demand-approve 新建位置（行号）和 T4 四要素清单
- _renderLifecycleBar 的实现方式（CSS 类或内嵌 <style>）
- 为 Plan 05 提供：unit-approved 状态的需求将在 demand-select 视图作为遴选候选
</output>
