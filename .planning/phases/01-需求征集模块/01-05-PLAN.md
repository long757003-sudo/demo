---
phase: 01-需求征集模块
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
autonomous: true
requirements: ["DEM-06", "DEM-07"]
must_haves:
  truths:
    - "切换到「信息办管理员」角色进入 demand-select 视图，看到 unit-approved 状态的需求候选"
    - "每条需求有「支持」和「不支持」两个按钮；点「支持」状态置 supported 并 toast；点「不支持」立即置 not-supported 并 toast，同时从右侧滑入 Drawer 展示 BR-04 六类原因完整原文（仅展示，无需选择）"
    - "遴选页底部有「提交遴选结果」按钮，点击后批量确认状态、logOperation、toast 成功"
    - "Phase 1 遴选页不显示「发起专家评审」入口"
    - "切换到任意需求相关角色进入 demand-list 视图，T2 列表可按状态和单位筛选，覆盖 draft/submitted/unit-pending/unit-approved/supported/not-supported 等状态"
  artifacts:
    - path: "my-project-wiki/raw/docs/demo-0408/shared/views/demand.js"
      provides: "demand-select（D-15 Drawer + D-16 无评审入口 + D-17 提交按钮）+ demand-list 筛选器和列对齐 UI-SPEC"
      contains: "registerView('demand-select'"
  key_links:
    - from: "demand-select 不支持按钮"
      to: "showDrawer BR-04 六类原因原文"
      via: "_dsRejectWithDrawer 立即改状态 + 展示 Drawer"
      pattern: "showDrawer"
    - from: "demand-list 筛选器"
      to: "DATA.demands"
      via: "按 status/unitId/关键词过滤重新渲染"
      pattern: "DATA\\.demands\\.filter"
---

<objective>
完成 Phase 1 闭环：改造 demand-select（DEM-06）落实 D-15/D-16/D-17，改造 demand-list（DEM-07）对齐 UI-SPEC 视图 H 筛选器和表格列。

Purpose: DEM-06 是 Phase 1 终点——信息办管理员对单位领导审批通过的需求做支持/不支持标记。D-15 关键：不支持点击后立即改状态并展示 Drawer（仅展示完整 BR-04 原文），区别于 UI-SPEC 旧版 Modal 选原因的描述，以 D-15 为准。DEM-07 为全流程提供列表视角。
Output: demand-select 和 demand-list 视图完成改造。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-需求征集模块/01-CONTEXT.md
@.planning/phases/01-需求征集模块/01-RESEARCH.md
@.planning/phases/01-需求征集模块/01-UI-SPEC.md
@.planning/phases/01-需求征集模块/01-03-SUMMARY.md
@.planning/phases/01-需求征集模块/01-04-SUMMARY.md
@my-project-wiki/raw/docs/demo-0408/shared/views/demand.js
@my-project-code/CLAUDE.md

<interfaces>
registerView('demand-select', ...) // demand.js line ~1333, 含 BR-04 基础实现
registerView('demand-list', ...)   // demand.js line ~793

showDrawer(title, bodyHtml): core.js
closeDrawer(): core.js
toast/showModal/closeModal
logOperation
deadlineClass(dateStr): string
projectTypeTag(type) / budgetToType(budget): string

DATA.demands 状态流转（Plan 04 结束后）：
unit-approved 记录可进入遴选 → Plan 05 置为 supported 或 not-supported
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 demand-select — D-15 Drawer + D-16 无评审入口 + D-17 提交按钮</name>
  <files>my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js (重点 line 1333-1525 demand-select 现有实现，识别并删除旧 BR-04 Modal 选原因交互、任何「发起专家评审」按钮)
    - my-project-wiki/raw/docs/demo-0408/shared/core.js (showDrawer、closeDrawer、logOperation 签名)
    - .planning/phases/01-需求征集模块/01-UI-SPEC.md (视图 G「信息办需求遴选页」章节；注意其描述 Modal + radio 选原因，被 D-15 覆盖)
    - .planning/phases/01-需求征集模块/01-CONTEXT.md (D-15 / D-16 / D-17)
    - .planning/phases/01-需求征集模块/01-RESEARCH.md (BR-04 六类原因完整原文章节、Code Examples「BR-04 Drawer 展示」、Pitfall 3 Drawer 不含确认按钮)
  </read_first>
  <action>
完整替换 `registerView('demand-select', ...)` 函数体。核心规则：
- 点击「支持」：直接置 status='supported' + toast + renderView 刷新
- 点击「不支持」：直接置 status='not-supported' + toast + showDrawer 展示 BR-04 六类原因完整原文（Drawer 仅展示，无按钮，无需用户选择；Pitfall 3）
- 禁止「发起专家评审」入口/按钮（D-16）
- 底部有「提交遴选结果」按钮（D-17）

视图结构：

```javascript
var BR04_REASONS = [
  '（一）已经启动建设，或已建成并正常运行的项目，再次报送需求申请重建且无正当理由的',
  '（二）项目需求不明确、不合理，市场调研不充分，预算经费严重背离实际，或无法确定牵头业务责任部门或分管领导的',
  '（三）项目需求与现有系统功能或数字资源有较大重叠，且无正当理由的',
  '（四）项目的建设理念和技术路线已经过时或属于当前落后并被行业或市场淘汰的',
  '（五）已经被否决的项目建设需求，且没有新的理由支持再次研究论证的',
  '（六）不符合学校其他相关规定的'
];

registerView('demand-select', function() {
  if (getCurrentRole() !== 'info-admin' && getCurrentRole() !== 'info-leader') {
    return '<div class="empty">请切换到信息办管理员或信息办领导角色</div>';
  }
  var batch = DATA.collectionPlans[0] || { title: '当前征集批次' };
  var all = DATA.demands.filter(function(d){
    return d.status === 'unit-approved' || d.status === 'in-selection' || d.status === 'supported' || d.status === 'not-supported';
  });
  var stats = {
    total: all.length,
    supported: all.filter(function(d){return d.status==='supported';}).length,
    rejected: all.filter(function(d){return d.status==='not-supported';}).length,
    pending: all.filter(function(d){return d.status==='unit-approved' || d.status==='in-selection';}).length
  };
  var filterUnit = window._dselUnit || 'all';
  var filterStatus = window._dselStatus || 'all';
  var kw = window._dselKw || '';
  var filtered = all.filter(function(d){
    if (filterUnit !== 'all' && d.unitId !== filterUnit) return false;
    if (filterStatus !== 'all' && d.status !== filterStatus) return false;
    if (kw && (d.name || '').indexOf(kw) < 0) return false;
    return true;
  });
  var rows = filtered.map(function(d) {
    var badge = d.status === 'supported' ? '<span class="tag tag-green">已支持</span>'
              : d.status === 'not-supported' ? '<span class="tag tag-red">不支持</span>'
              : '<span class="tag tag-gray">未处理</span>';
    return '<tr>' +
      '<td>' + (d.sortOrder || '') + '</td>' +
      '<td>' + d.name + '</td>' +
      '<td>' + (d.unitId || '') + '</td>' +
      '<td>' + (d.submitter || '') + '</td>' +
      '<td>' + (d.budget||0) + '万 ' + (typeof projectTypeTag==='function' ? projectTypeTag(budgetToType(d.budget||0)) : '') + '</td>' +
      '<td>' + badge + '</td>' +
      '<td class="desc">' + ((d.summary||'').slice(0,60)) + '</td>' +
      '<td>' +
        '<button class="btn btn-sm btn-primary" onclick="_dselSupport(\''+d.id+'\')">支持</button> ' +
        '<button class="btn btn-sm btn-danger" onclick="_dselReject(\''+d.id+'\')">不支持</button>' +
      '</td>' +
      '</tr>';
  }).join('');
  if (filtered.length === 0) rows = '<tr><td colspan="8" class="empty">当前批次暂无待遴选需求，请等待各单位完成审批后刷新。</td></tr>';

  return (
    breadcrumb('需求征集','需求遴选') +
    '<h2>需求遴选 — ' + batch.title + '</h2>' +
    '<div class="page-sub">总 '+stats.total+' 条｜已支持 '+stats.supported+' 条｜不支持 '+stats.rejected+' 条｜未处理 '+stats.pending+' 条</div>' +
    '<div class="card">' +
      '<div class="filters">' +
        '<select id="dsel-unit" onchange="_dselSetFilter(\'unit\',this.value)"><option value="all">全部单位</option><option value="unit-edu">教务处</option><option value="unit-sci">科研处</option><option value="unit-stu">学工处</option><option value="unit-hr">人事处</option><option value="unit-lib">图书馆</option></select>' +
        '<select id="dsel-status" onchange="_dselSetFilter(\'status\',this.value)"><option value="all">全部状态</option><option value="unit-approved">未处理</option><option value="supported">已支持</option><option value="not-supported">不支持</option></select>' +
        '<input id="dsel-kw" type="text" placeholder="搜索项目名称" oninput="_dselSetFilter(\'kw\',this.value)">' +
      '</div>' +
      '<table class="data-table"><thead><tr>' +
        '<th>序号</th><th>项目名称</th><th>单位</th><th>负责人</th><th>预算</th><th>状态</th><th>摘要</th><th>操作</th>' +
      '</tr></thead><tbody>' + rows + '</tbody></table>' +
    '</div>' +
    '<div class="actions actions-footer">' +
      '<button class="btn btn-primary" onclick="_dselSubmitSelection()">提交遴选结果</button>' +
    '</div>'
  );
});
```

配套函数：

```javascript
function _dselSetFilter(key, val) {
  if (key === 'unit') window._dselUnit = val;
  else if (key === 'status') window._dselStatus = val;
  else if (key === 'kw') window._dselKw = val;
  renderView('demand-select');
}

function _dselSupport(id) {
  var d = DATA.demands.find(function(x){return x.id===id;});
  if (!d) return;
  d.status = 'supported';
  logOperation('demand-select','support', d.name);
  toast('已标记为支持','success');
  renderView('demand-select');
}

function _dselReject(id) {
  var d = DATA.demands.find(function(x){return x.id===id;});
  if (!d) return;
  // D-15：立即改状态，然后展示 Drawer（仅展示原文，无确认按钮，Pitfall 3）
  d.status = 'not-supported';
  logOperation('demand-select','reject', d.name);
  toast('已标记为不支持','success');
  renderView('demand-select');
  var html = '<div class="br04-drawer">' +
    '<p class="br04-intro">根据《西南大学信息化项目管理办法》第十三条，下列情形不予支持：</p>' +
    BR04_REASONS.map(function(r){ return '<div class="br04-item">'+r+'</div>'; }).join('') +
  '</div>';
  showDrawer('不予支持原因说明（BR-04）', html);
}

function _dselSubmitSelection() {
  var pending = DATA.demands.filter(function(d){ return d.status === 'unit-approved'; }).length;
  var body = pending > 0
    ? '<p>仍有 '+pending+' 条需求未处理。提交后未处理的需求将保持为未处理状态，后续可继续遴选。</p><p>确认提交遴选结果？</p>'
    : '<p>所有需求已处理。提交后将进入需求库，无法撤回。</p>';
  showModal('确认遴选结果', body,
    '<button class="btn" onclick="closeModal()">取消</button>' +
    '<button class="btn btn-primary" onclick="_dselConfirmSubmit()">确认提交</button>');
}

function _dselConfirmSubmit() {
  var supported = DATA.demands.filter(function(d){return d.status==='supported';}).length;
  var rejected = DATA.demands.filter(function(d){return d.status==='not-supported';}).length;
  logOperation('demand-select','submit-selection','supported='+supported+',rejected='+rejected);
  closeModal();
  toast('遴选结果已提交，共 '+supported+' 条支持 / '+rejected+' 条不支持，通知已发送','success');
  renderView('demand-select');
}
```

D-16 严禁在 demand-select 视图中出现：「发起专家评审」按钮、navigate('review-launch')、navigate('expert-') 等跳转。若现有代码中有此类字符串须删除。

Pitfall 3 严禁：showDrawer 内部包含 `onclick="_dselConfirm` 或「确认不支持」按钮。Drawer 仅是展示性。
  </action>
  <verify>
    <automated>grep -c "_dselSupport\|_dselReject\|_dselSubmitSelection\|_dselConfirmSubmit\|_dselSetFilter" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js &amp;&amp; grep -c "BR04_REASONS" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 包含五个函数：`_dselSetFilter`、`_dselSupport`、`_dselReject`、`_dselSubmitSelection`、`_dselConfirmSubmit`
    - demand.js 包含 `BR04_REASONS` 数组且数组含 6 条字符串（grep 命中「已经启动建设」「需求不明确」「功能或数字资源有较大重叠」「技术路线已经过时」「已经被否决」「不符合学校其他相关规定」六条关键词）
    - demand.js 中 `_dselReject` 内部 d.status 赋值在 showDrawer 调用**之前**（确保立即改状态）
    - demand.js 中 showDrawer 调用的内容不包含字符串 `_dselConfirm`、`确认不支持`、`radio`（Drawer 仅展示）
    - demand.js 中 demand-select 视图不包含字符串 `发起专家评审`、`navigate('review-launch'`、`navigate('expert-` （D-16）
    - demand.js 中 demand-select 视图包含「提交遴选结果」按钮 onclick=`_dselSubmitSelection`（D-17）
    - demand.js 包含筛选 id：`dsel-unit`、`dsel-status`、`dsel-kw`
    - 手动：切换「信息办管理员」→ 需求遴选 → 点支持 → toast + Badge 变绿；点不支持 → toast + Badge 变红 + 右侧滑入 Drawer 展示六条完整原文 → Drawer 无确认按钮，点页面其他位置或关闭按钮可关闭 → 点「提交遴选结果」→ confirm modal → 确认 → toast
  </acceptance_criteria>
  <done>demand-select 落实 D-15（Drawer 展示）+ D-16（无评审入口）+ D-17（提交按钮），BR-04 六类原因为完整原文。</done>
</task>

<task type="auto">
  <name>Task 2: 改造 demand-list — T2 筛选器和列对齐 UI-SPEC 视图 H（DEM-07）</name>
  <files>my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</files>
  <read_first>
    - my-project-wiki/raw/docs/demo-0408/shared/views/demand.js (重点 line 793-1054 demand-list 现有实现；确认 Plan 03 是否已加入「新建一份需求」按钮调用 _dfStartNew)
    - .planning/phases/01-需求征集模块/01-UI-SPEC.md (视图 H「需求列表页」章节 筛选栏/表格列/截止日期规则)
    - .planning/phases/01-需求征集模块/01-CONTEXT.md (Claude's Discretion 第 2 项：筛选器 UI 展示、逻辑可简化)
  </read_first>
  <action>
改造 `registerView('demand-list', ...)` 的 render 函数。保留 Plan 03 已注入的「新建一份需求」按钮对 `_dfStartNew` 的调用。主要变更：

1. 筛选栏 UI-SPEC 对齐，包含以下控件（写在顶部 filters 区）：
   - `<input id="dl-kw">` 关键词搜索 placeholder='搜索项目名称或负责人' oninput `_dlSetFilter('kw',this.value)`
   - `<select id="dl-unit">` 按单位筛选 options: 全部单位 / unit-edu 教务处 / unit-sci 科研处 / unit-stu 学工处 / unit-hr 人事处 / unit-lib 图书馆 / unit-info 信息化办
   - `<select id="dl-status">` 按状态筛选 options：全部 / 草稿(draft) / 已提交(submitted) / 待排序(submitted) / 待审批(unit-pending) / 待遴选(unit-approved) / 已支持(supported) / 不支持(not-supported)
   - 若当前角色 === 'project-manager'：显示「新建一份需求」按钮 class `btn btn-primary` onclick `_dfStartNew()`

2. 筛选状态保存在 window 全局：`window._dlKw / _dlUnit / _dlStatus`

3. 表格列严格对齐 UI-SPEC 视图 H：
   - 项目名称（点击触发 navigate('demand-fill' 或详情 — 若 demand-detail 不存在则仅 toast「Demo：详情页待完善」）
   - 所在单位
   - 负责人
   - 预算（金额 + 项目类型 Badge）
   - 优先级（高/中/低）
   - 状态（Badge 颜色按 UI-SPEC Color 规则）
   - 提交时间（格式 YYYY-MM-DD）
   - 操作：按角色显示
     - project-manager：「编辑」「查看」
     - unit-leader：「查看」
     - info-admin：「遴选」（navigate('demand-select')）
     - 其他：「查看」

4. 分页：UI-SPEC 要求每页 10 条底部显示总条数。Demo 可简化为无真实分页，仅底部显示「共 N 条」文字（Claude's Discretion 允许简化）。

5. 状态 Badge 颜色映射（参考 UI-SPEC Color 章节）：
   - draft / 已关闭 → tag-gray
   - submitted / 待审批 → tag-orange
   - unit-pending → tag-orange
   - unit-approved / supported / 征集中 → tag-green
   - in-selection → tag-blue
   - not-supported / unit-rejected → tag-red

6. 配套函数：

```javascript
function _dlSetFilter(key, val) {
  if (key === 'kw') window._dlKw = val;
  else if (key === 'unit') window._dlUnit = val;
  else if (key === 'status') window._dlStatus = val;
  renderView('demand-list');
}

function _dlStatusBadge(status) {
  var map = {
    'draft': {cls:'tag-gray', text:'草稿'},
    'submitted': {cls:'tag-orange', text:'已提交'},
    'unit-pending': {cls:'tag-orange', text:'待审批'},
    'unit-approved': {cls:'tag-green', text:'待遴选'},
    'unit-rejected': {cls:'tag-red', text:'已退回'},
    'in-selection': {cls:'tag-blue', text:'遴选中'},
    'supported': {cls:'tag-green', text:'已支持'},
    'not-supported': {cls:'tag-red', text:'不支持'}
  };
  var m = map[status] || {cls:'tag-gray', text: status};
  return '<span class="tag '+m.cls+'">'+m.text+'</span>';
}
```

并在 render 函数中使用这些函数。不得改动 `_dfStartNew` 函数（Plan 03 已定义）。
  </action>
  <verify>
    <automated>grep -c "_dlSetFilter\|_dlStatusBadge" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js &amp;&amp; grep -c "dl-kw\|dl-unit\|dl-status" my-project-wiki/raw/docs/demo-0408/shared/views/demand.js</automated>
  </verify>
  <acceptance_criteria>
    - demand.js 包含函数 `_dlSetFilter` 和 `_dlStatusBadge`
    - demand.js 中 demand-list 视图包含 id: `dl-kw`、`dl-unit`、`dl-status`
    - demand.js 中 _dlStatusBadge 函数包含 status 映射：`draft`、`submitted`、`unit-pending`、`unit-approved`、`unit-rejected`、`in-selection`、`supported`、`not-supported` 全部 8 种（grep 8 种字符串各命中 ≥ 1）
    - demand.js 中 demand-list 视图使用 `_dlStatusBadge(d.status)` 或等价调用
    - demand.js 中 demand-list 视图对 project-manager 角色显示「新建一份需求」按钮 onclick `_dfStartNew`
    - demand.js 中 demand-list 视图包含「共 X 条」文字（页脚计数）
    - 手动：切换各角色进入需求列表，筛选器可按状态过滤（切换 status 后行数变化），可按单位过滤；切换到 project-manager 可见「新建一份需求」按钮，点击进入空白四步向导；切换到 info-admin 操作列显示「遴选」跳转 demand-select
  </acceptance_criteria>
  <done>demand-list 筛选器和列符合 UI-SPEC 视图 H，状态 Badge 覆盖 Phase 1 全部状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 角色 → 遴选操作 | demand-select render 入口仅放行 info-admin / info-leader |
| 不支持标记 → Drawer 展示 | Drawer 内容为静态规则原文，无用户输入 |
| 筛选条件 → DATA.demands 读取 | 仅读取不修改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Authorization | 非 info-admin 角色访问 demand-select | mitigate | render 入口检查 currentRole，不通过返回空状态 |
| T-05-02 | Integrity | _dselReject 改状态后 showDrawer 失败 | mitigate | 顺序：先 d.status = 改，后 renderView，最后 showDrawer，即使 Drawer 失败状态也已持久 |
| T-05-03 | XSS | d.name / d.summary 拼进 innerHTML | mitigate | Plan 03 已限制 name 和 summary 长度；本 Task 应避免直接 innerHTML 用户 HTML；若需严谨可添加 escape 函数（参考 core.js 是否已有 escapeHtml） |
| T-05-04 | Repudiation | 支持/不支持无历史 | mitigate | _dselSupport / _dselReject / _dselConfirmSubmit 均调用 logOperation |
| T-05-05 | Information Disclosure | BR-04 规则原文来自公开管理办法 | accept | 非敏感信息，属于演示要求的公开规则 |
| T-05-06 | Authorization (IDOR) | 筛选器暴露所有单位数据 | accept | Demo 级，角色切换演示用 |
</threat_model>

<verification>
1. 打开 app.html，切换「信息办管理员」
2. 进入需求遴选 → 看到 Plan 04 unit-approved 记录
3. 点「支持」一条 → Badge 绿，toast 成功
4. 点「不支持」一条 → Badge 红，toast 成功，同时右侧 Drawer 滑入展示 6 条 BR-04 完整原文（带「根据《西南大学信息化项目管理办法》第十三条」前言）
5. 确认 Drawer 内无「确认」或「取消」按钮
6. 页面中无任何「发起专家评审」按钮
7. 切换筛选器：单位/状态/关键词 → 列表过滤
8. 点「提交遴选结果」→ confirm modal（若未处理有提示）→ 确认 → toast
9. 切换至「需求列表」视图，筛选状态=已支持 → 显示支持的记录；切换到 project-manager → 可见「新建一份需求」按钮
10. 切换各角色验证操作列按钮不同
</verification>

<success_criteria>
- DEM-06 符合 D-15（立即改状态+Drawer 展示）/D-16（无评审入口）/D-17（提交按钮）
- DEM-07 筛选器和表格列符合 UI-SPEC 视图 H
- BR-04 六类原因为《管理办法》第十三条完整原文
- 所有 Phase 1 Success Criteria（ROADMAP 7 条）可端到端演示
</success_criteria>

<output>
创建 `.planning/phases/01-需求征集模块/01-05-SUMMARY.md` 记录：
- demand-select 改造后的关键函数和 D-15 交互验证
- BR-04 六条原文位置（行号）
- demand-list 筛选器字段 id 和状态 Badge 映射表
- Phase 1 全流程演示路径（信息办发起 → 单位指派 → 项目负责人填报 → 排序 → 领导审批 → 信息办遴选 → 列表查看）
- 遗留事项：生命周期进度条 CSS（Plan 04 若未完全落地）、性能优化等移至后续阶段
</output>
